const links=[{href:'/',text:'Home'},{href:'/menu',text:'Menu'},{href:'/#about',text:'About'},{href:'/#contact',text:'Contact'}];function updateAnchors(links,targetElementId){const ul=document.getElementById(targetElementId);const anchors=ul.querySelectorAll('a');links.forEach((link,index)=>{anchors[index].href=link.href;anchors[index].textContent=link.text})}
updateAnchors(links,'nav-links');document.querySelector('.menu-toggle').addEventListener('click',()=>{const navLinks=document.getElementById('nav-links');navLinks.classList.toggle('show')});document.querySelectorAll('.addToCart').forEach(button=>{button.addEventListener('click',()=>{const productName=button.getAttribute('data-name');const productPrice=button.getAttribute('data-price');let cart=JSON.parse(localStorage.getItem('cart'))||[];cart.push({name:productName,price:productPrice});localStorage.setItem('cart',JSON.stringify(cart));alert(`${productName} a été ajouté au panier!`)})});document.getElementById('Cart').addEventListener('click',()=>{const cartContent=document.getElementById('cart-content');const cartItems=document.getElementById('cart-items');const cartTotal=document.getElementById('cart-total');cartItems.innerHTML='';const cart=JSON.parse(localStorage.getItem('cart'))||[];let total=0;cart.forEach(item=>{const li=document.createElement('li');li.textContent=`${item.name} - $${item.price}`;cartItems.appendChild(li);total+=parseFloat(item.price)});cartTotal.textContent=total.toFixed(2);cartContent.style.display='block'});const closeCartButton=document.getElementById('close-cart');closeCartButton.addEventListener('click',()=>{document.getElementById('cart-content').style.display='none'});const clearCartButton=document.getElementById('clear-cart');clearCartButton.addEventListener('click',()=>{localStorage.removeItem('cart');document.getElementById('cart-items').innerHTML='';document.getElementById('cart-total').textContent='0.00'});const checkoutButton=document.getElementById('checkout');checkoutButton.addEventListener('click',()=>{if(localStorage.getItem('cart')){const orderId=Math.floor(Math.random()*1000000);alert(`Commande validée! Numéro de commande: ${orderId}`);localStorage.removeItem('cart');document.getElementById('cart-items').innerHTML='';document.getElementById('cart-total').textContent='0.00'}else{alert("Votre panier est vide. Ajoutez des articles avant de valider la commande.")}})